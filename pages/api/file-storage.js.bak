// API endpoint for file sharing/storage (demo)
import { secrets } from '../../lib/secrets';
import Sentry from '../../lib/sentry';
let files = [];

export default function handler(req, res) {
  try {
    if (req.method === 'POST') {
      const { fileId, userId, metadata } = req.body;
      if (!fileId || !userId || !metadata) {
        return res.status(400).json({ error: 'Missing fields', code: 400 });
      }
      files.push({ fileId, userId, metadata, ts: Date.now() });
      return res.status(200).json({ success: true });
    }
    if (req.method === 'GET') {
      return res.status(200).json(files);
    }
    return res.status(405).json({ error: 'Method Not Allowed', code: 405 });
  } catch (err) {
    console.error('File Storage API Error:', err);
    return res.status(500).json({ error: 'Internal Server Error', code: 500 });
  }
}
